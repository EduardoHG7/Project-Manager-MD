# SQLite por defecto: funciona sin instalar nada.
# Para producción con más de una instancia, usa Postgres:
# DATABASE_URL="postgresql://usuario:password@host:5432/motorshow"
DATABASE_URL="file:./dev.db"

# Genera uno con: openssl rand -base64 32
NEXTAUTH_SECRET="cambia-esto-por-un-secreto-aleatorio"
NEXTAUTH_URL="http://localhost:3000"
